{
  "name": "axis-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "latest",
    "prettier": "latest"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
